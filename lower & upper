%{
#include <stdio.h>
%}

%%
[a-z]   { printf("%c", yytext[0]-32); }
.       { printf("%s", yytext); }
\n      { printf("\n"); }
%%

int yywrap(){ return 1; }

int main() {
    yylex();
    return 0;
}

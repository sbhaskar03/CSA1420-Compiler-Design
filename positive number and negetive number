%{
#include <stdio.h>
%}

%%

-[0-9]+    { printf("Negative number = %s\n", yytext); }
[0-9]+     { printf("Positive number = %s\n", yytext); }
.          { /* ignore other characters */ }
\n         { /* ignore newline */ }

%%

int yywrap(){ return 1; }

int main() {
    yylex();
    return 0;
}
